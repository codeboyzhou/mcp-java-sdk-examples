{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "path": {
      "type": "string",
      "minLength": 1,
      "maxLength": 256,
      "description": "The directory path to read, required."
    },
    "pattern": {
      "type": "string",
      "minLength": 1,
      "maxLength": 256,
      "description": "Regular expression to filter files, optional, default is empty, means no filter."
    },
    "recursive": {
      "type": "boolean",
      "description": "Whether to list files recursively, optional, default is false."
    }
  },
  "required": [
    "path"
  ]
}
